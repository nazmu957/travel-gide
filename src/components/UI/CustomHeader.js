import React from "react";
import { Layout } from "antd";

const { Header } = Layout;

const CustomHeader = () => {
  return <Header className="header">{/* Your header content */}</Header>;
};

export default CustomHeader;
