import React from "react";
import { Layout } from "antd";

const { Footer } = Layout;

const CustomFooter = () => {
  return (
    <Footer className="footer">
      
    </Footer>
  );
};

export default CustomFooter;
